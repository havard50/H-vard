# One.com / Apache: feed data must not sit in cache for weeks (Netlify does this via netlify.toml headers).
<IfModule mod_headers.c>
  <Files "content.json">
    Header set Cache-Control "public, max-age=0, must-revalidate"
  </Files>
</IfModule>
